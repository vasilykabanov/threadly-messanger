version: '3.6'
services:
    mongo-auth:
      image: mongo:4.4
      container_name: mongo-auth
      restart: always
      environment:
        MONGODB_AUTH_DATABASE: threadly_auth_db
      ports:
        - "27017:27017"